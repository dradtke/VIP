To install vim projects as a separate filetype in Linux:
--------------------------------------------------------

Just run install-mime.sh.
Use uninstall-mime.sh to get rid of it.

It doesn't work yet in my tests due to a bug in xdg-mime,
but hopefully it'll be fixed soon.
